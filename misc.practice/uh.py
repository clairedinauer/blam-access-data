#This is the electronic checklist for participant 000001
import pandas as pd
import numpy as np

class B000001:

    def __init__(self, BeliefID)
        self.BeliefID = BeliefID

    def task(self, medhist, ccmed, nonccmed, nuq, ddf, scid5, scid5pd, matrics, hmi, bprs, chat, cains, lof, wamhi)
        self.medhist = medhist
        self.ccmed = ccmed
        self.nonccmed = nonccmed
        self.nuq = nuq
        self.ddf = ddf
        self.scid5 = scid5
        self.scid5pd = scid5pd
        self.matrics = matrics
        self.hmi = hmi
        self.bprs = bprs
        self.chat = chat
        self.cains = cains
        self.lof = lof
        self.wahmi = wahmi

Missing == 0
Completed == 1

df = 
